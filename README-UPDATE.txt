PLAYCROWS - BACKGROUND AND V2 REWARDS UPDATE

Apply this update to your working PlayCrows redesign after the PNG image fix.

1. Extract this ZIP.
2. Copy its src folder into your website project (the folder with package.json). Merge folders and replace matching files. Do not put src inside another src folder.
3. In GitHub Desktop, commit the changes and Push origin.
4. After your Vercel deployment finishes, refresh with Ctrl+F5.

Included:
- A subtle fixed Night Crows background illustration.
- Shared V1/V2 server information and rates.
- A V1/V2 selector for the two reward sections.
- Server-card Features links that select the correct rewards.
- V1 rewards retained exactly.
- V2 starter items, Day 3 gift-pack contents, and daily rewards.
- Updates in English, Korean, Thai, Portuguese, and Traditional Chinese.

The two Gold rewards stay separate. Deep Night's Wind Suit has no added quantity.

All image references retain the PNG repair. You do not need to apply the older image-fix patch again after this update.

If you have made further custom edits to these source files, merge the relevant changes rather than overwriting those custom edits.

For a static host, build again with pnpm build and deploy the resulting dist folder. The complete updated source package also contains a rebuilt dist folder.

Production build and TypeScript checks passed. V1 preservation, all V2 quantities, language coverage, and the bundled background image were checked. No live deployment was made.
